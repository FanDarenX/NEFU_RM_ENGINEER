FUNC void showdata(void){
	int idx;
	exec("log > data.log");
	for(idx=0;idx<2000;idx++){
		printf("%f\n",buff1[idx]);
	}
	for(idx=0;idx<2000;idx++){
		printf("%f\n",buff2[idx]);
	}
	for(idx=0;idx<2000;idx++){
		printf("%f\n",buff3[idx]);
	}
	exec("log off");
}
